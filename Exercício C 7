#include <stdio.h>

int main(void) {

  int i, j, soma=0, mat[2][3];

  for(i=0;i<2;i++){
    for(j=0;j<3;j++){
      printf("Informe o valor: ");
      scanf("%d", &mat[i][j]);
      soma+= mat[i][j];
    }
  }  
  printf("A soma de todos os elementos é: %d", soma);
  return 0;
}
