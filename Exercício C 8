#include <stdio.h>

int main(void) {

  int i, j, mult=1, mat[2][3];

  for(i=0;i<2;i++){
    for(j=0;j<3;j++){
      printf("Informe o valor: ");
      scanf("%d", &mat[i][j]);
      mult *= mat[i][j];
    }
  }  
  printf("O produto de todos os elementos é: %d", mult);
  return 0;
}
